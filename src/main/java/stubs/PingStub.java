package stubs;

//import static com.github.tomakehurst.wiremock.client.WireMock.*;
//import static com.github.tomakehurst.wiremock.client.WireMock.aResponse;

public class PingStub {
//  private String baseStubUrl =  System.getProperty("wiremock.hostname","http://localhost:9190");
//  private final String pingPath = "/ping";
//  {
//      getPing();
//  }
//
//  //Если будет get на {basePath}/ping, то мы возвращаем статус 200
//  private void getPing() {
//    stubFor(get(String.format("%s/%s",baseStubUrl, pingPath))
//            .willReturn(aResponse()
//                    .withStatus(200)));
//  }
}
